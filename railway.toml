[build]
builder = "DOCKERFILE"
dockerfilePath = "agents/backend/Dockerfile"
pathAsRoot = "agents/backend"

[deploy]
startCommand = "sh -c \"uv run uvicorn src.api.app:app --host 0.0.0.0 --port ${PORT:-8000}\""